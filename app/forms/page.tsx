import FormBuilderClient from "@/components/FormBuilderClient"

export const metadata = {
  title: "Form Builder | Ready Nest",
}

export default function FormsPage() {
  return <FormBuilderClient />
}
